source "https://rubygems.org"

# Pins Jekyll and plugin versions to match what GitHub Pages runs in production.
gem "github-pages", group: :jekyll_plugins

# Windows does not include zoneinfo files; tzinfo-data provides them.
gem "tzinfo-data", platforms: [:windows, :jruby]
